# AI Movie Recommendation Assistant

Интеллектуальный ассистент для персонализированных рекомендаций фильмов с использованием OpenAI API и векторных embeddings.

## Возможности

- 🤖 **AI-ассистент**: Естественный диалог для выяснения предпочтений
- 🎯 **Персонализация**: Рекомендации на основе ваших вкусов и настроения
- 👥 **Совместный режим**: Подбор фильмов для двух человек с учетом интересов каждого
- 📊 **Распределение 30-30-40**: В совместном режиме - 30% для первого, 30% для второго, 40% пересечение
- 🧠 **Embeddings**: Использование векторных представлений для точного поиска
- 💾 **История**: Сохранение оценок для улучшения будущих рекомендаций

## Архитектура

```
Vibecoding-Consultant/
├── main.py                      # Точка входа
├── config.py                    # Конфигурация
├── requirements.txt             # Зависимости
├── catalog_okko.parquet         # База фильмов Okko
│
├── database/                    # SQLite БД
│   ├── db_manager.py           # Управление БД
│   └── models.py               # Модели данных
│
├── ai/                         # AI модуль
│   ├── assistant.py            # OpenAI ассистент
│   └── prompt_templates.py     # Промпты
│
├── embeddings/                 # Векторные embeddings
│   ├── embedding_manager.py    # OpenAI Embeddings API
│   ├── vector_store.py         # Хранилище векторов
│   └── similarity.py           # Вычисление сходства
│
├── recommender/                # Рекомендательная система
│   ├── recommendation_engine.py # Основной движок
│   ├── content_filter.py       # Фильтрация контента
│   └── collaborative_session.py # Совместные сессии (30-30-40)
│
├── catalog/                    # Каталог фильмов
│   └── catalog_loader.py       # Загрузка и поиск
│
├── cli/                        # Консольный интерфейс
│   ├── console_ui.py           # UI
│   └── session_manager.py      # Управление сессиями
│
└── data/                       # Данные
    ├── users.db                # SQLite база
    ├── embeddings_cache.pkl    # Кеш embeddings
    └── sessions/               # Сессии
```

## Установка

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd Vibecoding-Consultant
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

Требуемые библиотеки:
- `pandas` - работа с данными
- `pyarrow` - чтение parquet файлов
- `openai>=1.0.0` - OpenAI API
- `numpy` - векторные операции
- `scikit-learn` - косинусное сходство
- `python-dotenv` - переменные окружения

### 3. Настройте OpenAI API

Создайте файл `.env` в корне проекта:

```env
OPENAI_API_KEY=your_openai_api_key_here
```

Получить API ключ можно на https://platform.openai.com/api-keys

### 4. Убедитесь, что файл catalog_okko.parquet находится в корне проекта

## Использование

### Запуск программы

```bash
python main.py
```

### Режимы работы

#### 1. Одиночный режим

Подбор фильмов для одного пользователя:

1. Выберите "1" при запуске
2. Введите ваше имя
3. Общайтесь с ассистентом о предпочтениях
4. Оцените предложенные фильмы (❤️ / 👎)
5. Получите персональную подборку

#### 2. Совместный режим

Подбор фильмов для двух человек:

1. Выберите "2" при запуске
2. Введите имена обоих пользователей
3. Каждый пользователь отвечает на вопросы ассистента
4. Каждый оценивает свои фильмы
5. Система формирует подборку:
   - **30%** - фильмы для первого пользователя
   - **30%** - фильмы для второго пользователя
   - **40%** - фильмы для совместного просмотра (пересечение интересов)

### Пример работы совместного режима

**Сценарий**: Пользователь 1 любит Марго Робби, Пользователь 2 - Киану Ривза

1. Ассистент спрашивает у первого: "Какой актер тебе интересен?"
2. Ответ: "Марго Робби"
3. Ассистент уточняет: "Какие жанры фильмов с Марго Робби тебе нравятся?"
4. Ответ: "Драма и комедия"
5. Повторяется для второго пользователя
6. Система выдает фильмы с Марго Робби (30%), с Киану Ривзом (30%), и драмы/комедии/боевики (40% пересечение)

## Как работает система embeddings

1. **Инициализация**: При первом запуске создаются embeddings для всех 18,130 фильмов
2. **Кеширование**: Векторы сохраняются в `data/embeddings_cache.pkl`
3. **Поиск**: Предпочтения пользователя преобразуются в вектор
4. **Сходство**: Вычисляется косинусное сходство между запросом и фильмами
5. **Пересечение**: Для совместного режима ищутся фильмы с высоким сходством к обоим пользователям

## Конфигурация

Настройки в `config.py`:

```python
# Модели OpenAI
OPENAI_MODEL = "gpt-3.5-turbo"              # Чат модель
OPENAI_EMBEDDING_MODEL = "text-embedding-3-small"  # Embedding модель

# Количество рекомендаций
INITIAL_RECOMMENDATIONS_COUNT = 10  # Для первичной оценки
FINAL_RECOMMENDATIONS_COUNT = 15    # Финальная подборка

# Распределение в совместном режиме
RECOMMENDATION_SPLIT = {
    "user1_preference": 0.30,  # 30%
    "user2_preference": 0.30,  # 30%
    "intersection": 0.40       # 40%
}

# Порог сходства
SIMILARITY_THRESHOLD = 0.7
```

## База данных

Система использует SQLite для хранения:
- **users** - пользователи и их предпочтения
- **ratings** - оценки фильмов (лайки/дизлайки)
- **sessions** - история сессий и диалогов

Файл БД: `data/users.db`

## Структура каталога фильмов

Файл `catalog_okko.parquet` содержит:
- **serial_name** - название фильма/сериала
- **genres** - жанры
- **content_type** - тип контента
- **country** - страна производства
- **actors** - актеры
- **director** - режиссер
- **age_rating** - возрастной рейтинг
- **studio_name** - студия
- **release_date** - дата выпуска
- **description** - описание
- **url** - ссылка на Okko.tv

Всего: **18,130 фильмов и сериалов**

## Команда разработки

- Артём
- Катя
- Елисей

## Требования

- Python 3.10+
- OpenAI API ключ
- ~2 GB свободного места (для кеша embeddings)

## Примечания

- Первый запуск может занять время (генерация embeddings)
- Embeddings кешируются для последующих запусков
- API запросы к OpenAI оплачиваются согласно тарифам
- Фронтенд будет добавлен в следующих версиях

## Лицензия

MIT

## Поддержка

При возникновении проблем создайте Issue в репозитории.
